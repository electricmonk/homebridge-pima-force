/**
 * Homebridge plugin metadata. The PLATFORM_NAME must match `pluginAlias`
 * in config.schema.json and the `platform` value users put in their
 * Homebridge config.json.
 */
export const PLATFORM_NAME = 'PimaForce';
export const PLUGIN_NAME = 'homebridge-pima-force';
